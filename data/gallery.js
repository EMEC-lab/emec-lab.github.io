/* =========================================================================
 * data/gallery.js
 * 갤러리. 앨범 단위로 관리한다. HTML 에 하드코딩 금지.
 *
 *   id     고유 식별자
 *   title  앨범명
 *   date   'YYYY-MM-DD'  정렬 기준(최신순)
 *   cover  대표 이미지 경로. 비우면 images[0].src 를 쓴다
 *   images [{ src, caption }, ...]
 *
 * 이미지 규칙 (CLAUDE.md 8번)
 *   - 긴 변 1600px 이하, WebP 또는 압축 JPG
 *   - 썸네일은 400px 로 별도 생성
 *   - 모든 이미지는 alt 와 loading="lazy" 를 붙여 렌더링된다
 *
 * TODO: 아래는 구조 확인용 자리표시자. 실제 앨범으로 교체할 것.
 * ========================================================================= */

const GALLERY = [
  {
    id: "album-1",
    title: "[앨범명 — 추후 확정]",
    date: "2026-04-24",
    cover: "",
    images: []
  }
];
